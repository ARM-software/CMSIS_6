layer:
  type: App
  description: Validation of CMSIS-Core implementation (Bootloader part)

  # packs:
  #   - pack: ARM::CMSIS

  misc:
    - for-compiler: CLANG_TI
      Link:
        # Reference TI Arm Clang Compiler ToolUser's Guide, v5.1, Section 6 Cortex-M Security Extensions (CMSE)
        - -Xlinker --import_cmse_lib_out=implib.o

  groups:
    - group: Source Files
      files:
        - file: ./bootloader.c
